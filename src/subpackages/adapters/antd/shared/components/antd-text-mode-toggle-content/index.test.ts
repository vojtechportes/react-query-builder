import * as subject from './index';

describe('#adapters/antd/shared/components/antd-text-mode-toggle-content/index', () => {
  it('exposes the intended adapter exports', () => {
    expect(Object.keys(subject).sort()).toEqual(['AntdTextModeToggleContent']);
  });
});
